@model ContosoUniversity.Models.CountryLicenseType
@{
    ViewBag.Title = "Delete";
}
<h2>Delete</h2>
<h4>Country / License Type</h4>

<p class="error">@ViewBag.ConcurrencyErrorMessage</p>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.CountryLicenseTypeName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.CountryLicenseTypeName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.IsVisible)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.IsVisible)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.CountryLicenseTypeId)

        <div class="form-actions no-color">
            @Html.ActionLink("Back to List", "Index")
            <input type="submit" value="Delete" class="btn btn-danger" />
        </div>
    }
</div>
